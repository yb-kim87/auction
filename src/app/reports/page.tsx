"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { AppHeader, HEADER_BTN } from "@/components/AppHeader";
import { createServiceReport, fetchMyProfile, fetchServiceReports, type ServiceReport } from "@/lib/api";
import type { UserProfile } from "@/types/auction";
const allowed=new Set(["student","consulting_student","consultant","admin"]);
export default function ReportsPage(){const [profile,setProfile]=useState<UserProfile|null>(null);const [rows,setRows]=useState<ServiceReport[]>([]);const [f,setF]=useState({type:"bug",title:"",description:""});const [msg,setMsg]=useState("");useEffect(()=>{fetchMyProfile().then(setProfile).catch(()=>{});fetchServiceReports().then(setRows).catch(e=>setMsg(e.message));},[]);if(profile&&!allowed.has(profile.role))return <><AppHeader/><main className="p-8"><h1 className="text-2xl font-bold">서비스 제보</h1><p>수강생 이상 등급부터 이용할 수 있습니다.</p></main></>;const submit=async(e:React.FormEvent)=>{e.preventDefault();try{const r=await createServiceReport(f);setRows([r,...rows]);setF({type:"bug",title:"",description:""});setMsg("제보가 접수되었습니다.")}catch(e){setMsg(e instanceof Error?e.message:"제보에 실패했습니다.")}};return <><AppHeader nav={<><Link className={HEADER_BTN} href="/">추천 물건</Link><Link className={HEADER_BTN} href="/assignments">과제</Link></>}/><main className="mx-auto max-w-4xl space-y-6 p-6"><h1 className="text-2xl font-bold">서비스 버그·개선 제보</h1><form onSubmit={submit} className="space-y-3 rounded-xl border bg-card p-5"><select className="rounded-lg border p-3" value={f.type} onChange={e=>setF({...f,type:e.target.value})}><option value="bug">버그 제보</option><option value="improvement">개선 제안</option></select><input className="w-full rounded-lg border p-3" required placeholder="제목" value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><textarea className="min-h-40 w-full rounded-lg border p-3" required placeholder="상세 내용을 입력해주세요" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/><button className="rounded-lg bg-primary px-4 py-3 text-primary-foreground">제보 등록</button>{msg&&<p className="text-sm text-muted-foreground">{msg}</p>}</form>{rows.map(r=><article key={r.id} className="rounded-xl border bg-card p-4"><b>{r.title}</b><span className="ml-3 text-xs text-muted-foreground">{r.status}</span><p className="mt-2 whitespace-pre-wrap text-sm">{r.description}</p></article>)}</main></>}
